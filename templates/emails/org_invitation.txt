You've been invited to join {{ organization_name }} on {{ site_name }}

{{ inviter_username }} invited you to join "{{ organization_name }}" as {{ role_display }}.

Accept the invitation here:

{{ accept_url }}

This invitation expires on {{ expires_at }}. If you weren't expecting this, you can safely ignore it.
